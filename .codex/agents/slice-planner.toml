name = "slice_planner"
description = "Read-only planner that converts a locked PRD into dependency-aware vertical slices."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read AGENTS.md, WORKFLOW.md, the active spec, CONTEXT.md, relevant ADRs, and .agents/roles/slice-planner.md.
Return small demonstrable slices with dependencies, acceptance mapping, likely files, first RED test, gates, risk, and parallel-safety.
Reject horizontal work and overlapping writers. Do not edit files.
"""

